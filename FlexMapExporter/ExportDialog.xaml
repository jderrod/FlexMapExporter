<Window x:Class="FlexMapExporter.ExportDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Flex-Map Exporter" 
        Height="350" 
        Width="600"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" 
                   Text="Export Flex-Map Configuration" 
                   FontSize="18" 
                   FontWeight="Bold" 
                   Margin="0,0,0,20"/>

        <!-- Output Folder Selection -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" 
                       Text="Output Folder:" 
                       VerticalAlignment="Center" 
                       Margin="0,0,10,0"/>
            
            <TextBox Grid.Column="1" 
                     x:Name="OutputPathTextBox"
                     VerticalAlignment="Center" 
                     Padding="5"
                     IsReadOnly="True"/>
            
            <Button Grid.Column="2" 
                    Content="Browse..." 
                    Click="BrowseButton_Click"
                    Padding="15,5"
                    Margin="10,0,0,0"/>
        </Grid>

        <!-- Description -->
        <TextBlock Grid.Row="2" 
                   Margin="0,0,0,15"
                   TextWrapping="Wrap"
                   Foreground="#666">
            This tool will automatically analyze your family parameters and export a complete configurator package including:
            • Parameter definitions and formulas
            • Geometry meshes
            • Parameter-to-geometry influence mapping
        </TextBlock>

        <!-- Status Display -->
        <GroupBox Grid.Row="3" 
                  Header="Export Progress" 
                  Margin="0,0,0,15"
                  Padding="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="StatusTextBlock" 
                           TextWrapping="Wrap"
                           FontFamily="Consolas"
                           FontSize="11"/>
            </ScrollViewer>
        </GroupBox>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="4" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="Export" 
                    Click="ExportButton_Click"
                    IsDefault="True"
                    Padding="25,8"
                    Margin="0,0,10,0"
                    FontWeight="Bold"/>
            <Button Content="Cancel" 
                    Click="CancelButton_Click"
                    IsCancel="True"
                    Padding="25,8"/>
        </StackPanel>
    </Grid>
</Window>
